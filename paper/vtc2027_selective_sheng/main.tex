\documentclass[conference]{IEEEtran}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,bm,booktabs,graphicx,cite}
\usepackage[hidelinks]{hyperref}
\newcommand{\E}{\mathbb{E}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\pos}[1]{[#1]_+}
\newcommand{\norm}[1]{\lVert#1\rVert}
\newcommand{\G}{\mathsf{G}}
\newcommand{\SIC}{\mathsf{S}}
\newcommand{\Sel}{\mathsf{Select}}
\input{generated/values.tex}
\title{Blind Disturbance-Model Selection for\\Diffusion-Aided Semantic Image Reception}
\author{\IEEEauthorblockN{Author information pending}
\IEEEauthorblockA{Affiliations and correspondence to be supplied}}
\begin{document}
\maketitle
\begin{abstract}
Explicit interference reconstruction is useful only when its additional
modeling assumptions improve the desired image estimate. With unknown
interference power, a joint diffusion sampler may instead introduce errors
and unnecessary computation. We propose a blind disturbance-model selector
for a frozen semantic image codec. Before diffusion inference, the receiver
uses excess received energy and block-energy heterogeneity to choose either
Gaussian denoising or joint signal--interference reconstruction. The rule is
fitted on separate development images; only the selected branch is executed,
and no neural weights are updated. We evaluate the rule on CelebA images with
CIFAR-10 interference, using identical received signals for all receiver
comparisons and a joint receiver with the same bypass as an additional control.
\input{generated/abstract_result.tex}
Separately trained DeepJSCC and MambaJSCC codecs provide external performance
references under the same channel-use budget.
\end{abstract}
\begin{IEEEkeywords}
Semantic communications, interference cancellation, diffusion models,
receiver adaptation, deep joint source--channel coding.
\end{IEEEkeywords}
\DraftNotice

\section{Introduction}
Wireless image reception under co-channel interference requires recovering
the desired image from a mixture whose interference power and structure are
unknown. Deep joint source--channel coding (JSCC) maps images directly to
channel symbols, enabling gradual quality degradation as noise increases
\cite{bourtsoulatze2019deep}. SwinJSCC and MambaJSCC extend this approach with
transformer and state-space architectures \cite{yang2025swinjscc,wu2024mambajscc}.
A deployed codec must handle interference without changing the transmitted
representation or incurring unnecessary inference cost.

Generative receivers address this problem by using learned signal priors.
CDDM denoises the received latent after matching the channel disturbance to
a diffusion noise level \cite{wu2024cddm}; ICDM explicitly reconstructs the
desired and interfering latents jointly \cite{wu2026icdm}. Iterative source
error correction \cite{pmlr-v206-lee23c}, channel-conditioned diffusion
reconstruction \cite{wang2025diffcom}, and blind inverse-problem inference
\cite{murata2023gibbsddrm} further motivate adapting recovery at the receiver.
A receiver equipped with Gaussian denoising and joint reconstruction must
still choose a disturbance model for each observation.

Always choosing joint reconstruction assumes that exploiting interference
structure offsets the errors introduced by estimating it. This need not hold:
the receiver must infer two latent signals and an unknown amplitude from one
mixture, using imperfect priors and finite-step sampling. An inaccurate
interference estimate can distort the desired signal while adding computation.
Gaussian denoising avoids this estimation burden, but discards potentially
useful structure. Can a receiver retain the benefit of joint reconstruction while avoiding it
when Gaussian denoising would recover the image more accurately?

The key insight is that modeling more interference structure introduces both
information and estimation uncertainty; the appropriate model therefore
depends on the received frame. This motivates selecting model complexity
before reconstruction. Excess received energy measures the aggregate
disturbance relative to known signal and noise powers, while variation across
blocks describes how unevenly energy is distributed. These observable
statistics can inform the choice without identifying the interfering image.
Desired-signal variation also affects these statistics, so their usefulness
requires validation.

We propose a blind disturbance-model selector based on these two statistics.
Image-group validation chooses a threshold rule on separate development
images, retaining a fixed receiver if adaptation gives no validation gain.
The rule is then frozen and executes only the selected branch, avoiding the
cost of running both receivers. All neural weights remain fixed, so the
comparison isolates the receiver decision from codec retraining.

On 256 independent image pairs, selection improves mean PSNR by 0.750 dB
over blind Gaussian denoising and 1.334 dB over calibrated joint sampling,
with 34.7\% fewer predictor evaluations than joint sampling. A same-gate
control separates model selection from merely bypassing joint inference.
Harmful decisions and block mismatch delimit the benefit. External DeepJSCC
and MambaJSCC codecs provide reference points; the shared-prior receiver
adaptations do not reproduce the complete published CDDM/ICDM systems.

The contributions are as follows:
\begin{itemize}
\item We formulate blind disturbance-model selection for a frozen semantic
codec, making receiver choice an explicit reconstruction decision.
\item We develop an energy-based rule that selects one receiver before
generative inference using $O(n)$ calculations and no neural retraining.
\item We establish a mean-quality and computation benefit on independent
paired observations, while identifying harmful decisions and mismatch sensitivity.
\end{itemize}
Sections II and III present the system model and selector; Sections IV and V
report experiments and conclusions.

\section{System Model and Problem Formulation}
In this section, we model image encoding, transmission, diffusion-assisted
latent recovery and image decoding, then formulate receiver selection.

\subsection{Image Representation and Transmission}
Let $\bm u\in[0,1]^d$ be an RGB image, with $d=3H_{\rm img}W_{\rm img}$.
The JSCC encoder $f_s$ combines representation learning and channel mapping;
$f_z$ encodes an interfering image $\bm v$ into the same dimension:
\begin{equation}
 \bm x=\mathcal N(f_s(\bm u)),\qquad
 \bm z=\mathcal N(f_z(\bm v)),\quad
 \mathcal N(\bm t)=\frac{\sqrt n\,\bm t}{\norm{\bm t}}.
 \label{eq:encode}
\end{equation}
Nonzero codewords have $n$ complex symbols and unit average power. The
bandwidth ratio is $n/d$. Partition the symbols into $B$ blocks of lengths
$L_b$, where $\sum_bL_b=n$. Transmission gives
\begin{equation}
 \bm y_b=\bm x_b+a_b\bm z_b+\bm w_b,\qquad
 \bm w\sim\mathcal{CN}(\bm0,N_0\bm I),\quad a_b\ge0 .
 \label{eq:channel}
\end{equation}
Here $a_b$ is the unknown block interference amplitude and $N_0$ the complex
noise power. Desired-channel gain is unity and symbol alignment is known.
The receiver knows $N_0$, normalization and block boundaries, but not
$\bm u$, $\bm z$ or $a_b$. The simulator sets nominal interference power to
$10^{-\gamma/10}-N_0$ for nominal SINR $\gamma$; realized block energies of
$\bm z$ make per-frame SINR differ from this setting.

\subsection{Diffusion-Assisted Image Recovery}
Recovery combines frame-specific evidence in $\bm y$ with learned signal
and interference priors. Joint reconstruction uses
\begin{equation}
 \begin{split}
 p(\bm x,\bm z\mid\bm y,\bm a)&\propto p_s(\bm x)p_z(\bm z)\\
 &\quad\times\exp\!\left[-\frac{1}{N_0}\sum_b
 \norm{\bm y_b-\bm x_b-a_b\bm z_b}^{2}\right].
 \end{split}
 \label{eq:joint}
\end{equation}
Amplitudes require estimation, and a matching mixture alone cannot identify
both latents: $(\bm x_b+a_b\bm\delta_b,\bm z_b-\bm\delta_b)$ gives
the same observation. Alternatively, Gaussian recovery uses $p_s$ with
\begin{equation}
 p_{\G}(\bm y\mid\bm x;\nu)=
 \mathcal{CN}(\bm y;\bm x,\nu\bm I),\qquad \nu\ge N_0 .
 \label{eq:gaussian}
\end{equation}
Gaussian and joint receivers $\mathcal R_{\G}$ and $\mathcal R_{\SIC}$ use
frozen diffusion priors with observation-based initialization or guidance,
not exact posterior sampling. Only joint recovery estimates a second latent.
A blind rule
$\pi_\theta(\bm y)\in\{\G,\SIC\}$ selects one branch, followed by the common decoder:
\begin{equation}
 \widehat{\bm u}_\theta
 =g_s\!\left(\mathcal N\!\left(\mathcal R_{\pi_\theta(\bm y)}(\bm y)\right)\right).
 \label{eq:decode}
\end{equation}

\subsection{Quality, Cost, and Receiver Selection}
We optimize image reconstruction using per-image PSNR, $q(\bm u,\widehat{\bm u})=
-10\log_{10}(\|\bm u-\widehat{\bm u}\|^2/d)$ for clipped RGB outputs.
LPIPS and MS-SSIM supplement fidelity, not downstream task accuracy. Cost
counts both priors' predictor calls; latency includes selection and decoding.

For development pairs $\mathcal D=\{(\bm u_i,\bm y_i)\}_{i=1}^{M}$, we fit
\begin{equation}
 \theta^\star\in\arg\max_{\theta\in\Theta}
 \frac{1}{M}\sum_{i=1}^{M}q(\bm u_i,\widehat{\bm u}_\theta(\bm y_i)),
 \label{eq:objective}
\end{equation}
where $\Theta$ is the family retained by image-group validation, including
a constant-receiver fallback. Bandwidth, power and neural weights are fixed;
cost is reported separately. This fitting is offline. Online, the frozen
rule executes one branch without access to its reconstruction error,
motivating the observable-statistic selector in Section III.

\section{Blind Disturbance-Model Selection}
In this section, we describe the two candidate receivers and develop a blind
selection rule that chooses one receiver before diffusion inference.

\subsection{Candidate Receivers}
Both receivers use the same frozen desired-signal prior and decoder.
For Gaussian reception, estimate the disturbance from raw received symbols:
\begin{equation}
 \begin{gathered}
 e_b=\frac{\norm{\bm y_b}^2}{L_b},\qquad
 \bar e=\frac{1}{n}\sum_b L_be_b,\\
 \widehat P=\pos{\bar e-1-N_0},\qquad
 \widehat\nu=N_0+\widehat P.
 \end{gathered}
 \label{eq:energy}
\end{equation}
With unit source power and vanishing signal--interference cross expectation,
unclipped excess energy estimates mean interference energy. Finite blocks,
latent correlations, and clipping prevent treating it as an exact amplitude
estimate. In particular, local desired-signal energy can resemble interference
heterogeneity.

Let $r(\bm y)\in\R^{2n}$ stack real and imaginary components. Given the training
diffusion schedule $\bar\alpha_t$, the Gaussian receiver $\G$ starts at the
nearest noise level satisfying
\begin{equation}
 \frac{1-\bar\alpha_{t_0}}{\bar\alpha_{t_0}}
 \simeq\frac{\widehat\nu}{2},\qquad
 \bm v_{t_0}=\sqrt{\bar\alpha_{t_0}}\,r(\bm y).
 \label{eq:match}
\end{equation}
The factor $1/2$ converts complex noise power to variance per real coordinate.
For decreasing respaced indices, a noise predictor $\epsilon_s$ gives
\begin{align}
 \widehat{\bm x}_0&=
 \frac{\bm v_t-\sqrt{1-\bar\alpha_t}\epsilon_s(\bm v_t,t)}
 {\sqrt{\bar\alpha_t}},\\
 \bm v_{t'}&=\sqrt{\bar\alpha_{t'}}\widehat{\bm x}_0+
 \sqrt{1-\bar\alpha_{t'}}\epsilon_s(\bm v_t,t).
 \label{eq:gaussian_update}
\end{align}
At most 40 predictor evaluations are used, with fewer when the matched initial
index is below 39.

The structured receiver $\SIC$ initializes
$a_b^{(0)}=\sqrt{\pos{e_b-1-N_0}}$ and performs joint diffusion inference with
signal and interference predictors. During late corrector updates it obtains
denoised, frame-normalized estimates $\widetilde{\bm x}$ and
$\widetilde{\bm z}$, then updates
\begin{equation}
 a_b^{+}=\pos{\frac{
 \Re\{\widetilde{\bm z}_b^{H}
       (\bm y_b-\widetilde{\bm x}_b)\}}
 {\norm{\widetilde{\bm z}_b}^{2}}}.
 \label{eq:amplitude}
\end{equation}
An empty denominator retains the previous amplitude. This is a nonnegative
least-squares update conditional on point estimates, not a correction for
their uncertainty. Calibration begins when the signal coefficient
$\alpha(t)\ge0.5$. Estimated block SINR updates the inherited guidance
coefficients by clipped linear interpolation of the original AWGN table.
We use the existing 40-step, order-three joint sampler: 40 signal and 40
interference evaluations. The initialization-only variant keeps amplitudes
fixed and supplies an ablation of calibration.

\subsection{Selection Before Generative Inference}
The selector augments $\widehat P$ with normalized block-energy variation
\begin{equation}
 H=\frac{B^{-1}\sum_{b=1}^{B}(e_b-\bar e)^2}
          {\max(\bar e^2,\varepsilon)}.
 \label{eq:heterogeneity}
\end{equation}
Here $\varepsilon>0$ prevents division by zero, and all receiver blocks have
equal length. $H$ measures energy variation, not the interference distribution. For
$\theta=(\tau_\ell,\tau_h,\eta)$, define
\begin{equation}
 \pi_\theta(\bm y)=
 \begin{cases}
 \G,&\widehat P\le \tau_\ell,\ H\le\eta,\\
 \G,&\widehat P\le \tau_h,\ H>\eta,\\
 \SIC,&\text{otherwise}.
 \end{cases}
 \label{eq:selector}
\end{equation}
We also test the reversed assignment of $\G$ and $\SIC$ in
\eqref{eq:selector}; the Gaussian branch is not assumed to belong to the
low-power region. The energy-only family sets $\tau_\ell=\tau_h$. Either family can reduce to a
constant receiver; heterogeneity need not be useful.

We fit thresholds to mean per-image PSNR on development data. Eight folds keep
all channel conditions belonging to one image together. Each fold is predicted
using thresholds fitted on the other seven, then the family with the highest
out-of-fold mean is refitted on all development images. Ties prefer lower
component inference cost and then the simpler family. If neither family
improves on the better fixed receiver in development validation, that fixed
receiver is retained. No threshold is adjusted using confirmation or pressure
results. At reception, \eqref{eq:selector} is computed first and only its chosen
branch is executed. Selection adds $O(n)$ energy calculations and no diffusion
evaluations.

For analysis, let $q_j(\bm u,\bm y)$ be a candidate's PSNR. The unavailable
per-frame oracle has quality $\max(q_\G,q_\SIC)$. The exact identity
\begin{equation}
 \max(q_\G,q_\SIC)-q_{\pi_\theta}
 =|q_\G-q_\SIC|\,
 \mathbf1\{\pi_\theta\ne\arg\max_j q_j\}
 \label{eq:regret}
\end{equation}
shows that complementary receivers are necessary but not sufficient: mistakes
on large-gap frames matter most. This is an accounting identity, not a
generalization bound. Mean PSNR is the fitting objective; neither mean MSE,
LPIPS nor harmful-tail probability is implicitly optimized.

\section{Experimental Evaluation}
In this section, we evaluate reconstruction quality and inference cost against
fixed receivers and external codecs, followed by tests of harmful decisions
and interference mismatch.

\subsection{Experimental Setup}
Desired images are $128\times128$ CelebA center crops \cite{liu2015celeba};
$32\times32$ CIFAR-10 interference images are nearest-neighbor upsampled.
Training uses 162,770 desired and 50,000 interference images. The frozen
SwinJSCC codec has stage widths $(128,192,256)$, depths $(2,2,6)$, and an
$8\times16\times16$ real latent: 1,024 complex symbols at CBR $1/48$.
Each DiT prior has 28 blocks, width 1,152, 16 heads and patch size two.
The inherited training schedule uses 40/100 desired/interference-codec epochs
and 50/20 desired/interference-prior epochs; selection adds no neural training.

DeepJSCC and MambaJSCC direct-codec references each train desired and
interference codecs for 40 epochs at SNR 20 dB and CBR $1/48$, using one seed
and validation MSE for checkpoint selection. DeepJSCC uses a pinned third-party
PyTorch reproduction; MambaJSCC uses official code. Public checkpoints are
also evaluated without fine-tuning, with one encoder processing both image
streams. Public MambaJSCC differs in training domain, channel and resolution;
public DeepJSCC additionally uses CBR $0.16256$. Their results are transfer
diagnostics. The SwinJSCC-specific diffusion priors cannot be attached to
these codecs without new prior training; complete published CDDM/ICDM systems
are not reproduced here.

Development uses 64 validation pairs and seed 1700. Independent confirmation
uses 256 test pairs, seeds 2700/2701, SNR 20 dB, and nominal SINRs
$\{-7,-4,0,4,7,10\}$ dB. Sixteen 64-symbol blocks have stationary,
alternating $(0.25,1.75)$, or burst $(0,2)$ relative interference powers.
The 18 conditions give 9,216 frames per receiver. Confirmation follows policy
freezing. File hashes exclude overlap with development, pressure tests and
200 previously evaluated pairs; independence is at image-pair, not identity,
level. The power grid is $\{0,0.1,0.2,0.35,0.5,0.75,1,1.5,2,3,4,8,10^6\}$;
the heterogeneity grid is $\{0.05,0.1,0.2,0.4,0.8,1.6\}$, giving 26 energy-only
and 2,028 two-feature rules across both assignment directions.

Controls include direct decoding, Gaussian reception using $N_0$ alone,
blind Gaussian reception, initialization-only and calibrated joint sampling,
and the selector. The same-gate control uses direct decoding for
$\widehat P\le0.15$ and calibrated joint sampling otherwise. Its outputs and
component times are reconstructed from paired branches, excluding gate
overhead. Development also tests coupled feedback and a Gaussian receiver
given nominal disturbance power, a privileged diagnostic. All SwinJSCC variants
share received tensors and sampler seeds; external codecs share image pairs,
conditions and channel seeds but generate their own codewords.

Measurements use float32, batch one, TF32 disabled, four CPU threads and an
RTX 4090. Synchronized receiver time includes selection, sampling and decoding;
encoding, metric computation and loading are excluded. The number of function
evaluations (NFE) counts calls to both priors.
Quality metrics are per-image PSNR and MSE, LPIPS-VGG v0.1 \cite{zhang2018lpips},
and four-scale MS-SSIM \cite{wang2003msssim}, averaged arithmetically after RGB
clipping to $[0,1]$; four scales fit the image resolution. Paired 95\% intervals
use 5,000 bootstrap resamples of image pairs, retaining both seeds and all
conditions. A harmful decision loses more than 0.5 dB against its comparator.

\subsection{Frozen Rule and Receiver Comparison}
\begin{table}[t]
\centering
\caption{Reconstruction quality and receiver cost on the confirmation set.}
\label{tab:main}
\input{generated/main_table.tex}
\end{table}
\begin{figure}[t]
\centering
\ResultFigure{figures/confirmation_quality.pdf}
\caption{Mean PSNR versus nominal SINR over the three interference profiles.}
\label{fig:quality}
\end{figure}

\begin{figure}[t]
\centering
\ResultFigure{figures/paired_tail.pdf}
\caption{Empirical CDF of per-frame PSNR change relative to calibrated joint sampling.}
\label{fig:tail}
\end{figure}

\input{generated/development_result.tex}
\input{generated/confirmation_result.tex}
In Fig.~\ref{fig:quality}, solid curves use the frozen SwinJSCC latent space,
whereas dashed curves use the separately retrained codecs.

\subsection{Inference Cost and Harmful Decisions}
\input{generated/tail_result.tex}
The CDF in Fig.~\ref{fig:tail} uses received frames; its vertical line marks
the $-0.5$ dB harmful-loss threshold.


\subsection{Block Mismatch and No Interference}
Pressure tests use 64 further test pairs and seed 3700, with nominal SINR
$-4,4,10$ dB. One profile shifts burst boundaries by half a receiver block;
another draws segment lengths uniformly from 16 to 128 symbols and normalizes
the binary on/off amplitude pattern. A separate SNR-20 condition has no
interference. The receiver keeps its original block partition and frozen rule.
These families are reported separately from matched confirmation.
\input{generated/pressure_result.tex}
For a visual check of confirmation, Fig.~\ref{fig:qualitative} shows all six
prespecified examples: image pairs
512 and 527, each under conditions 0, 8 and 15; none was selected by quality.
\begin{table}[t]
\centering
\caption{Mean PSNR under block mismatch and no interference.}
\label{tab:pressure}
\input{generated/pressure_table.tex}
\end{table}
\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/qualitative_prespecified.pdf}
\caption{Reconstructed images for six prespecified confirmation examples.}
\label{fig:qualitative}
\end{figure}

\section{Conclusion}
\input{generated/conclusion_result.tex}
These gains are population-specific: harmful decisions remain, and block
mismatch nearly removes the advantage over Gaussian denoising. Future work
will address fading, synchronization errors, and task-oriented evaluation
for vehicular links.
\bibliographystyle{IEEEtran}
\bibliography{references}
\end{document}
