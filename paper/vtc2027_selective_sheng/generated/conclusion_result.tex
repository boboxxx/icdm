On independent image pairs, the frozen selector improves mean PSNR over both blind Gaussian denoising and calibrated joint sampling. It also improves on the same-gate control. The same-gate result shows that the gain cannot be explained solely by bypassing joint inference.
