Table~\ref{tab:pressure} shows a +0.010 dB selector change against Gaussian reception for shifted blocks and +0.563 dB for random lengths. With no interference, selection matches Gaussian reception and avoids the joint receiver's loss. These are diagnostic populations; no rule is refitted or result pooled with matched confirmation.
